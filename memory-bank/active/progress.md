# Progress

**Complexity:** Level 1
**Task ID:** add-dependabot

## Build

Added `.github/dependabot.yaml` copied from Texarkanine/tab-yeet (weekly Monday 09:00 UTC-style time, npm groups `production-deps` / `development-deps` with minor+patch only, GitHub Actions group `github-actions`, assignee Texarkanine, commit prefixes `fix(deps)` / `chore(deps-dev)` / `chore(deps-ci)`).

No product code or tests changed — configuration only.
