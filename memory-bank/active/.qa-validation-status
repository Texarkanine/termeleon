PASS

QA re-review (2026-09-13) of terminal-selection-visibility after the QA-rework-2 build (contrast-ratio clear-on-disable fix).

Findings:
- The previously blocking defect is fixed: applyContrastRatio (src/apply.ts) now clears a previously-written minimumContrastRatio: 1 when setMinimumContrastRatio is false, called from both applyPalette and applyPalettePair (so Reapply, Import, and Mirror all pick it up). Regression coverage added: "reapply with setMinimumContrastRatio false clears a 1 written by a prior apply" and "setMinimumContrastRatio false leaves a custom contrast other than 1" in test/host/apply.test.ts. Both pass.
- Full suite green: 83 parser + 32 discovery + 8 cache + 46 extension-host tests (44 prior + 2 new regression tests), all passing.
- Fill semantics, authored-selection precedence, no-invented-foreground, Xresources highlight mapping, per-target last-apply with live-preview isolation, pair reapply, remove-clears-record, managedKeys, and not-live setting copy in package.json/README/STORE all re-checked against the plan and remain correct.
- No debris, TODOs, or stray duplicate content found in modified files.

Advisory (non-blocking): systemPatterns.md's "Surgical Settings Ownership" section documents that removeApplied clears a stray minimumContrastRatio: 1, but doesn't mention that applyPalette/applyPalettePair (and therefore Reapply) now do the same when the setting is toggled off. Optional follow-up, not required for acceptance.
