PASS

Semantic review of the rework build against the Level 2 rework plan (settings-category split, override rename, polarity inversion). Re-ran the complete suite: 85 parser + 32 discovery + 8 cache + 47 extension-host tests, all passing.

Findings:
- Mapping hub (`src/palette.ts`): `overrideIncludedSelectionForeground` inverts correctly (off honors authored fg, on omits it, inert when the theme has none); `overrideMissingSelectionHighlight` keeps prior fill semantics under the new name, authored highlight still wins unconditionally. Matches plan unit 1.
- Apply shell (`src/apply.ts`, `src/extension.ts`): option names renamed end-to-end with no leftover `includeSelectionForeground` / `fillMissingSelection` references in source; extension defaults match the plan (`overrideMissingSelectionHighlight: true`, `overrideIncludedSelectionForeground: false`). Matches plan unit 2.
- `package.json`: `contributes.configuration` is now a two-entry array, titles exactly "Picker Behavior" (order 1) and "Color Preferences when Applying New Themes" (order 2), property membership matches the plan's split, both override defaults correct, old ids absent. Matches plan unit 3.
- Docs (`README.md`, `STORE.md`): both rewritten to describe the two settings groups, the inverted foreground default, and the highlight-override naming; no residual mentions of the old ids. Matches plan unit 4.
- Tests: every behavior in the plan's Test Plan is present and exercises both polarities (default-writes-fg, override-omits-fg, inert-with-no-fg, highlight on/off/inert/no-background, apply and Reapply for both overrides including the Ghostty-pair reapply flip that preflight required, the two-category contract test, and the old-ids-absent assertion).
- No debug artifacts, TODOs, skipped/`.only` tests, or stray references to removed setting ids found in the diff.
- Preflight's non-blocking advisory (a settings-to-ApplyOptions adapter) was correctly left undone — it was explicitly optional and out of scope for this contained rework.

No findings block acceptance.
