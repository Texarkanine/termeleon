PASS

QA - issue-1-kitty-inline-comments - 2026-08-31

Reviewed: src/parsers/kitty.ts, test/parsers.test.ts (81c88c9..430875c) against memory-bank/active/projectbrief.md and tasks.md.

Findings:
- KISS: PASS. The dead ternary is replaced by one `.replace(/\s+#.*$/, '').trim()`. No new helper, no new abstraction.
- DRY: PASS. No duplication introduced. `parseXresources` is untouched and does not need the same strip (its comment marker is `!`, and a trailing `#` there is not a comment).
- YAGNI: PASS. No speculative parameters, options, or unused code.
- Completeness: PASS. All three requirements met: comments stripped, leading-`#` hex preserved, regression test covering both. Acceptance criteria 1-3 satisfied; `npm run test:parsers` reports 12 passed.
- Regression: PASS. Follows the parser conventions in the module (trim, key lowercase, `normalizeColor` at assignment). Full-line comment skip at line 18 still handles `# ...` lines. Constraint honored: change is isolated to `parseKitty`; `normalizeColor` and other parsers unchanged.
- Integrity: PASS. No magic numbers, placeholders, or debug artifacts. Comment on the strip explains the non-obvious part (why a leading `#` is not treated as a delimiter).
- Documentation: PASS. No project doc asserts kitty inline-comment behavior; README/docs contain no claim to update.

Advisories (non-blocking):
- The strip requires whitespace before `#`, so a pathological `color1 #cc6666# red` keeps the comment and drops the slot. kitty itself requires whitespace-delimited comments, so this is correct-by-spec, not a defect.
- `package-lock.json` is untracked and not in `.gitignore`. Pre-existing repo state, unrelated to this fix; decide separately whether to commit or ignore it.
