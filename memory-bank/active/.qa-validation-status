PASS
Task: issue-6-extension-host-tests (Level 2)
Date: 2026-08-31
Reviewed: 2a8a11d..5e24efc (build-phase diff) plus a full `npm test` run

Verification
- `npm test` exit 0: parser suite green, host suite 17 passing (smoke 1, LivePreview 2, apply/remove/snapshot 14).
- All 14 planned apply/remove/snapshot behaviors and both planned preview behaviors have a corresponding test. No stubs, TODOs, debug artifacts, or dead exports introduced.
- `git status` clean after a host run: `out/`, `.vscode-test/`, and the fixture `test/host/fixtures/workspace/.vscode/` are ignored, so the suite cannot leave the tree dirty.

Advisories (non-blocking)
1. Regression (minor): extracting `LivePreview` dropped the unconditional `clearTimeout` the old inline picker did in `onDidHide`. On accept, a pending debounced apply is left armed and fires unawaited alongside the final `applyPalette` in `pickAndApply`. Harmless in practice (the pending palette is the accepted item's, so the write is idempotent), and untested. Recommend a `commit()`/`stop()` on `LivePreview` that clears the timer without restoring, called on the accept path.
2. Documentation drift: `memory-bank/systemPatterns.md` still asserts "there is no extension-host test harness" and `memory-bank/techContext.md` describes only the parser harness. Both are now false. Preflight already routed this to Reflect/Archive; it must actually happen there.
3. Acceptance criterion 4 (`Fixes #6` in a commit message) is not yet satisfied — the only commits are `chore: saving work before <phase>`. Carry into the final commit.
4. Test-hygiene nits: `ansi()` in `test/host/preview.test.ts` builds a color by concatenation plus `.slice(0, 7)`, which obscures the value it produces; the `assert.notStrictEqual(..., '#111111')` on the next lines is redundant after the `strictEqual(..., '#222222')` above it.
5. `cancel restores the pre-session snapshot` drives `applyPalette` directly rather than `schedule`, so it exercises snapshot restore but not the schedule-then-cancel path. The debounce test does cover schedule-then-cancel, so coverage is not missing.
6. Plan said `afterEach` restore at both targets; build used `setup` + `suiteTeardown` with `resetSettings()` (both targets, colorCustomizations and minimumContrastRatio). Equivalent isolation.
