PASS
QA phase: ghostty-autodetect-pairs (Level 2) - 2026-08-31 (re-review after Build rerun)
Mechanical: `npm run compile` clean; `npm run test:parsers` 21/21 pass (10 new).

PRIOR BLOCKING - RESOLVED
1. Ownership leak across apply modes: FIXED. `applyPalette` (src/apply.ts:61) now strips previously owned keys via the same `stripOwnedKeys` as `applyPalettePair` (src/apply.ts:99). The helper moved to vscode-free `src/palette.ts` and the pair-then-flat shadowing scenario is covered by the new `stripOwnedKeys` test.
2. README settings keys: FIXED. README now says the scopes are the *values* of `workbench.preferredDarkColorTheme` / `workbench.preferredLightColorTheme` with concrete examples (`[One Dark Pro]`, `[GitHub Light]`). `memory-bank/productContext.md` Key Constraints updated to match.

ADVISORY (carried over / residual)
3. DRY: the `^(\[[^\]]+\])\.(.+)$` regex and scoped delete/prune walk still exist in both `stripOwnedKeys` (palette.ts) and `removeApplied` (apply.ts). Not directly mergeable - `removeApplied` needs per-key removal counts and a fallback sweep of unowned scoped blocks - but the shared regex could still be extracted. Non-blocking.
4. `preferredPairScopes` still falls back to `''`, yielding a `[]` scope if a preferred theme resolves empty. Near-unreachable (VS Code ships schema defaults for both settings), but fails silently into a bogus block. Non-blocking.
5. Trivial: `src/palette.ts` and `test/parsers.test.ts` lost their trailing newline in the rebuild. Cosmetic only.

RESOLVED ADVISORY
- Prior advisory 5 (KISS, inline-config branch): FIXED via a `hasInline` boolean; the `activeNames` size-check set is gone from `discoverGhostty`.

Next: QA PASS - proceed per the Level 2 workflow (Reflect).
