PASS

Semantic review of build-rework commit `b559fa7` (diff base `d8e8a20`, the commit `dep-fixes-tsc` actually branched from — an earlier local diff against a divergent, unpushed local-only `release 0.8.0` commit falsely suggested `CHANGELOG.md`/`.release-please-manifest.json` regressions; re-based on the correct merge-base, the real diff touches only `.github/dependabot.yaml`, `package.json`, `package-lock.json`, `tsconfig.json`, `test/host/picker.test.ts`, and memory-bank files — no `src/` changes).

Independently re-ran all four plan verification commands, all green:
- `npm run compile` — TypeScript 6.0.3, `bundler`/`commonjs`/`types: ["node"]`, zero errors.
- `npx tsx test/parsers.test.ts` — 85/85, including the `ci` section (`typescript >=7.0.0` and `@types/node >=23.0.0` ignores only — confirmed by reading the test source).
- `npm run package` — vsce engines check passes with `@types/vscode` `~1.75.0` (resolved 1.75.1) against `engines.vscode` `^1.75.0`; generated `.vsix` deleted after.
- `npm run compile-tests && npm run test:host` — 47/47 passing, after setting `XDG_RUNTIME_DIR` to a writable temp dir (the sandbox's default `/run/user/1000` doesn't exist; this is a QA-sandbox artifact, not a code or CI issue — CI does not run `test:host` per `techContext.md`).

Verified the specific finding from the prior QA FAIL is now corrected: `techContext.md`'s Package bullet (not the `ci`/Testing Process bullet) now states the `@types/vscode` tilde-pin and Dependabot ignore, and explicitly notes "vsce compares those declared ranges; the parser `ci` tests do not" — matching what the test source actually asserts.

Checked every plan unit against the diff: Unit 1 (`.github/dependabot.yaml` ignore with rationale comment matching the existing `@types/node` comment's style, `package.json`/lockfile tilde-pin) — done. Unit 2 (TypeScript `^6.0.0`, `moduleResolution: bundler`, `types: ["node"]`, no `ignoreDeprecations`, no `nodenext`, clean-tree lockfile regen) — done, no source call-site fixes were needed (fallback branch never triggered, consistent with preflight's empirical finding). Unit 3 (techContext record) — done and now correctly scoped.

KISS/DRY/YAGNI: no new abstractions, no reinvented utilities, no speculative code. The Dependabot comment mirrors the existing `@types/node` entry's phrasing and placement exactly — consistent with the established pattern, not a new one.

Completeness: no stubs, TODOs, or pseudocode. No requirement or acceptance criterion in `projectbrief.md` is unaddressed.

Regression / advisory (carried forward, not blocking): `test/host/picker.test.ts` pins `scopeToActiveTheme` to `false` in the mirror-preview suite's `setup`/`suiteTeardown`. This is outside the three written plan units, but is a minimal, documented, test-only fix for a suite that was failing locally since the unrelated `#54` `scopeToActiveTheme` default flip (not a TypeScript 6 regression) — necessary to satisfy the plan's own `npm run test:host` verification gate. Product code and defaults are untouched. Accepted, as in the prior QA pass.

Integrity: `~1.75.0` and `>=1.76.0` are the plan's deliberate policy values (VS Code API-floor pin), not magic numbers or shortcuts. No debug artifacts, no hardcoded test-only bypasses in product code.

Documentation: `techContext.md` is now internally consistent — the Package bullet claims exactly what vsce enforces; the Testing Process `ci` bullet claims exactly what the parser suite asserts. `projectbrief.md`/`tasks.md` need no further edits.

Working tree is clean (`git status --porcelain` empty) after verification; no stray `.vsix` or other build artifacts left behind.
