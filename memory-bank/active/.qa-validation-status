PASS

## QA Findings — import-picker-ux-fix

* `swatch()` and all `\u2588` block glyphs removed from `toItem`/`toMirrorItem` in `src/extension.ts`; detail lines are now plain origin paths (single and pair). Matches requirements 1-2. Non-blocking.
* `ensureTerminalVisible()` added and wired into `pickAndApply()`, called before the import Quick Pick opens, scoped to the import picker only (matches projectbrief requirement 3, which does not ask this of the mirror command). Non-blocking.
* Implementation is minimal: reuses an existing terminal (active, else first) or creates one; no speculative options/parameters added (YAGNI/KISS clean). Non-blocking.
* `test/host/picker.test.ts` covers: theme-item detail has no swatch glyphs, mirror pair-candidate detail has no swatch glyphs, mirror single-candidate detail has no swatch glyphs, and `ensureTerminalVisible` results in at least one terminal existing. Verified: full run of `npm run test:parsers` (50 passing) and `npm run test:host` (22 passing, including the 4 new picker tests) both green in this review. Non-blocking.
* Advisory: the new test only asserts a terminal exists afterward: it does not separately exercise the "reuse existing terminal" vs. "create new terminal" branches of `ensureTerminalVisible`. Acceptance criteria ("Import command reveals an integrated terminal when executed") is satisfied regardless. Advisory only, does not block.
* Advisory: `ThemeItem`/`MirrorItem` interfaces were made `export`ed alongside the functions that already needed exporting for the test import; the interfaces themselves aren't imported by the new tests. Harmless surface widening, consistent with exporting the functions that return them. Advisory only, does not block.
* No `systemPatterns.md` update required: no new durable, system-wide contract was introduced by this fix.
