PASS

## Findings

- No blocking or fixable issues found. All three findings from the prior `FAIL (fixable)` run are closed in the current plan:
  - Reapply-pair test is now an explicit stub ("the same for a Ghostty pair (both preferred-theme scopes rewritten)") and appears in the Behaviors list.
  - Live-preview isolation in `test/host/preview.test.ts` is now stated as mandatory, not conditional ("a mandatory isolation case").
  - Import's last-apply recording is correctly placed in `pickAndApply` immediately after its committed `applyPalette` call, matching the real control flow in `src/extension.ts` (`commandImport` never sees the accepted palette).
- TDD Plan Encoding: all four Implementation Plan units correctly order test-writing before code for the two executable units in `src/palette.ts`/`test/parsers.test.ts` and `src/apply.ts`+`src/extension.ts`/`test/host/*.ts`; the Xresources unit follows the same red/green structure; the doc unit is correctly unlabeled for tests (prose/policy).
- Convention Compliance: touched files respect the vscode-free-core / vscode-bound-shell split in `systemPatterns.md` — mapping/fill logic stays in `src/palette.ts`, `LastApply`/Memento/Reapply command stay in `src/apply.ts` and `src/extension.ts`. `LastApply` persisted per-target in the matching Memento matches the existing `termeleon.ownedKeys` pattern.
- Dependency Impact: verified every call site of `toColorCustomizations` (`src/apply.ts`, `test/parsers.test.ts`, `test/host/apply.test.ts`, `test/host/preview.test.ts`) is compatible with an additive, opt-in `fillMissingSelection` option; none breaks with the option omitted.
- Conflict Detection: no existing luminance/fill/last-apply/reapply utility exists to duplicate. Confirmed only `kitty`, `ghostty`, `iterm2`, and `toml.ts` (Alacritty/WezTerm) already map native selection fields; Xresources genuinely lacks the mapping (fixed by this plan) and Windows Terminal/MobaXterm formats genuinely have no selection fields (correctly left to the generic fill instead of per-parser invention).
- Completeness Precheck: all 7 project-brief requirements and 7 acceptance criteria have concrete implementation steps mapped to files and functions.

## Advisory

- Minor: the Test Plan's "MobaXterm parse: still no selection slots on the Palette" behavior isn't named as an explicit stub-test step in Unit 1 or Unit 2 (it's already true today and requires no production change, but adding one assertion line to the existing MobaXterm parse test would make the coverage claim in the Test Plan literally traceable).
- The Implementation Plan's phrase "`highlightbackground` as an alias for background" (Unit 2, step 2) is ambiguous — the Challenges & Mitigations section clarifies it actually means an alias for the *selection* background (`highlightColor`), not `p.background`. Worth a one-word clarification in the plan text before Build, though the correct behavior is already unambiguous from context.
- Radical/structural idea (advisory only, not applied): the luminance-based fallback-overlay logic being built for missing selection colors is really a general "derive a plausible VS Code color from the rest of the Palette when the source format omits it" primitive. Structuring `fallbackSelectionColors` as one case of a small `deriveMissingColor(key, palette): string | undefined` dispatch table now — even though only the selection case ships this task — would let future work (e.g. a missing `terminalCursor.foreground` from Windows Terminal, which also lacks cursor color) reuse the same luminance/overlay math instead of re-deriving it. Cost: a slightly more general internal shape in `palette.ts` for one call site today. Benefit: the next "format X doesn't have color Y" bug fixes into a one-line dispatch entry instead of a new bespoke fallback function. Evaluate against whether a second such gap is expected soon; if not, today's single-purpose `fallbackSelectionColors` is the right size.
