PASS WITH ADVISORY

TDD Plan Encoding: PASS. Unit 1 (executable) orders stub tests, stub `parseItermColorPresets`, red tests, then production code. Unit 2 is prose/policy (`README.md`, `STORE.md`, `memory-bank/productContext.md`, `memory-bank/systemPatterns.md`) and correctly has no tests. No change-detector steps. No TDD reordering.

Prerequisites: Level 2 plan is complete. No creative-phase docs are required or present.

Convention: Proposed files match the vscode-free core (`src/parsers/iterm2.ts`, `src/discover.ts`) and existing Node assert suites (`test/parsers.test.ts`, `test/discover.test.ts`). `parseItermColorPresets` returning `{ name, palette }[]` matches `parseWindowsTerminal`. Bundled paths under `/Applications/iTerm.app` and `~/Applications/iTerm.app` match Ghostty's darwin app-bundle scan. Discovery stays silent on missing files.

Dependency: `discover.ts` already imports from `src/parsers/iterm2.ts` and calls `discoverIterm2` from `discoverThemes`; the picker and Mirror consume that list with no extra vscode-bound work. Host tests under `test/host/` are unaffected. Updating `parseItermColors` to accept `<string>` floats also changes how user `.itermcolors` files parse — that is intended and covered by the listed parser tests.

Conflict: No overlap with WezTerm (compiled-in / Lua) or Windows Terminal package `defaults.json`. Current docs and `systemPatterns.md` still claim iTerm2 built-ins are undiscoverable; Unit 2 is supposed to correct that. Public contract is the VS Code commands and settings, not a published JS API.

Completeness: Brief requirement to integrate non-brittle built-ins maps to ColorPresets.plist parse + darwin discovery. Active iTerm2 profile colors and WezTerm/Windows Terminal built-ins stay documented limits. `parseItermColors` today only matches `<real>` component tags; Tango Light/Dark in the real plist use `<string>` floats, so that parser change is load-bearing.

Advisory: Discover tests cannot see `/Applications/iTerm.app` on Linux CI. Plant a small ColorPresets.plist fixture under `$HOME/Applications/iTerm.app/Contents/Resources/` (discover tests already replace `HOME`) and assert on that origin, and keep grammar coverage in `test/parsers.test.ts` with inline XML (one `<real>` preset, one `<string>` preset). Do not assert on result length while Darwin still walks a real iTerm.app.

Advisory: `parseItermColors` uses a non-greedy `<dict>…</dict>` match that is safe for flat `.itermcolors` and wrong for ColorPresets.plist (first match binds a preset name to only its first color block). `parseItermColorPresets` must slice each top-level preset with balanced dict matching (or equivalent), then call `parseItermColors` on that inner XML.

Advisory: Specify scan order. User `*.itermcolors` under Application Support should keep winning over bundled names if a shared `seen` Set is introduced; the plan's Set is only described for the two app-bundle plists.

Advisory (radical innovation): Also treat a `ColorPresets.plist` found in `extraDirs` as a preset document, the way extra dirs already work for walkable formats. That makes bundled-preset discovery testable on every CI OS without ungating macOS-only `/Applications`, and lets a user point at a copied plist without installing iTerm2.
