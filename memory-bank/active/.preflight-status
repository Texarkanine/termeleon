PASS WITH ADVISORY

# Preflight - investigate-mobaxterm-themes - 2026-09-02

## Findings

**TDD plan encoding passes.** Both executable units explicitly order empty test stubs before interface stubs, implemented tests and a red run before production implementation, and focused green runs afterward. The documentation unit is prose/policy and correctly schedules no tests. The source-enum assertion protects a cross-file user contract rather than detecting intentional prose changes.

**The earlier OneDrive finding is resolved.** Discovery now plans plain Documents, `%OneDrive%\Documents`, `%USERPROFILE%\OneDrive\Documents`, and `%APPDATA%\MobaXterm` in an explicit priority order; it adds a OneDrive discovery case, restores all mocked Windows environment variables, deduplicates overlapping roots, and marks only the first usable default-root `MobaXterm.ini` active. The documentation unit names the covered OneDrive path and the `-i` / portable extra-directory escape hatch.

**Conventions and public boundaries pass.** `src/parsers/mobaxterm.ts` remains in the vscode-free core, emits the existing `Palette`, and uses a byte-component helper beside `fromFloatComponents` without widening `normalizeColor`. The source takes `extraDirs`, `discoverThemes` remains the failure boundary, and no change is planned to the apply path or `extensionKind`.

**Dependency and integration coverage passes.** The plan accounts for parser exports, discovery registration and filename handling, `package.json` source configuration and keywords, `SOURCE_LABELS`, parser and discovery fixtures, environment-test isolation, README and store support tables, and persistent product/architecture context. Manifest and label edits remain in the write-code step so their test is red first.

**Conflict detection passes.** No existing parser handles INI sections or byte RGB triples. Ghostty's extension-agnostic extra-directory walk cannot produce a usable palette from MobaXterm's `[Colors]` keys, while resolved-path deduplication prevents overlapping default roots from listing the same MobaXterm file twice.

**Requirement coverage passes.** The plan implements the on-disk parser/discovery outcome, tests parser and discovery behavior, documents built-in-index, session, syntax, portable, and `-i` limits, and preserves the `Palette`, apply, and UI-kind contracts.

## Advisory - radical innovation

Consider extracting the planned section-scoped INI loop into a minimal `src/parsers/ini.ts` helper that returns keys for one named section. Keep `fromByteComponents` in `palette.ts`, then make `parseMobaXterm` only a key-to-palette mapping. This adds a small reusable seam for later PuTTY-family support (`Colour0` through `Colour21` use the same decimal triples) without changing this task's discovery or public contracts. This is advisory only; the current bespoke parser plan is acceptable as-is.
