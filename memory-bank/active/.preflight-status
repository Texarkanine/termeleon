FAIL (fixable)

## Findings

1. **Completeness gap (fixable, primary reason for FAIL)** — Unit 3's file list names `memory-bank/productContext.md` and `memory-bank/systemPatterns.md`, but its three numbered sub-steps only describe edits to the README.md/STORE.md capability table and notes. Neither persistent file has a concrete edit mapped to it. `systemPatterns.md`'s "Best-Effort Discovery" paragraph already documents per-emulator active-detection nuances (Ghostty theme lines, kitty `current-theme.conf`, Windows Terminal `profiles.defaults.colorScheme`, MobaXterm first-usable-default-root) but says nothing about Alacritty. After this rework, Alacritty gains comparably intricate active-detection logic (config-vs-import, Windows vs Unix roots, last-usable-import-wins) that a future maintainer touching `discoverAlacritty` would need to know about — exactly the "damage without this knowledge" bar that section applies to the other emulators. Re-plan should add a concrete sub-step appending an Alacritty clause there, and should explicitly state whether `productContext.md` needs a corresponding change or confirm no change is needed (its Key Constraints paragraph currently only lists format *gaps*, not active-detection behavior, so it may need nothing — but the plan should say so instead of leaving the file listed with zero action).

2. **Minor / advisory-leaning** — "Config file" identification for the new active-detection logic is unspecified beyond "for every discovered or default-root `alacritty.toml`". The current (soon-to-be-replaced) code identifies a config via the suffix regex `/alacritty\.toml$/i`, which already misfires on `test/fixtures/extra/extra-alacritty.toml` (a theme file, not a config) — latent today only because no test asserts `active` for that fixture. Recommend the re-plan state explicitly that a "config" is identified by exact basename (`path.basename(file).toLowerCase() === 'alacritty.toml'`), not a suffix match, so extraDirs theme files whose names happen to end in `alacritty.toml` are never treated as an importable/active config. Not blocking on its own — the acceptance criteria and test list make the intended behavior clear enough that a careful implementer would likely land here anyway — but worth pinning down explicitly since it silently changes correctness.

3. **Minor** — Unit 1's write-code step says resolution handles both `~/` and `~\` prefixes, but the Test Plan's behavior list only calls for a `~/` case. Either add a `~\` test case or drop that extra branch from the write-code step; as written it introduces one untested code path.

4. **Radical Innovation (advisory, not applied)** — Kitty's `kitty.conf` supports an `include` directive for splitting config across files, structurally identical to Alacritty's `import` / `[general].import` (a config file that points at another file holding the real color definitions). `discoverKitty` does not follow `include` today, so a kitty user who splits colors into an included file has the same "Mirror finds nothing" gap this task fixes for Alacritty. Extracting the spec-resolution half of `resolveAlacrittyImport` (tilde-expansion, absolute-path passthrough, relative-to-config-file) into a small shared helper now — rather than writing it Alacritty-only — would make a future kitty `include` fix a small addition instead of a second from-scratch implementation. Concretely: `resolvePathSpec(spec: string, configFile: string, home: string): string` in a shared location (e.g. `src/parsers/paths.ts`), with `resolveAlacrittyImport` becoming a thin wrapper (or an alias). Recorded as an idea for the operator to weigh against scope creep for this task; not applied here.

## Other checks (no findings)

- TDD Plan Encoding: both executable units (alacrittyImports/resolveAlacrittyImport; discoverAlacritty) place stub tests → stub interface → write tests (red) → write code (green), in that order. No change-detector tests present.
- Convention Compliance: file locations (`src/parsers/toml.ts`, `src/discover.ts`, `test/parsers.test.ts`, `test/discover.test.ts`) match existing per-emulator parser/discovery/test layout. `withFixtureHome`'s `APPDATA` isolation (already present for MobaXterm) is reused correctly rather than duplicated.
- Dependency Impact: `test/parsers.test.ts`'s "extra directories are scanned for every walkable format" case only asserts `name`, not `active`, for the alacritty fixture, so it stays green without modification — correctly left off the plan's touched-file list. `mirrorCandidates`/`pickMirrorCandidate` already tolerate more than one simultaneously-active same-source theme (precedent: Windows Terminal stable+preview), so the plan's lack of a MobaXterm-style "first-active-wins" guard across Alacritty config roots is not a new class of bug.
- Conflict Detection: no existing generic path-resolution helper (`isAbsolute`/`win32`/`posix`) exists elsewhere in `src/` to duplicate; `alacrittyImports`/`resolveAlacrittyImport` are new, non-overlapping names. `Palette`, `apply`, and `extensionKind` are untouched, consistent with the constraint.
- Completeness Precheck (Alacritty-specific requirements): parser and discovery requirements are each mapped to concrete files/functions/algorithm; only the two persistent-doc files above lack that mapping.
