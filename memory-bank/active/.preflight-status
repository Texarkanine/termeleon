PASS WITH ADVISORY

Preflight run 2026-08-31 for task vsix-github-releases (Level 2).

Prerequisites: tasks.md shows test planning, implementation plan, technology validation, and pre-mortem complete. Level 2, so no creative-phase documents required. Plan and design decisions read.

TDD Plan Encoding: PASS. The single executable unit ("Release and package contract") orders its steps stub-tests -> stub-interface -> write-tests-run-red -> write-code-run-green; an implementer cannot follow it code-first. Unit 2 (README + techContext) is prose/policy and correctly owes no tests. No change-detectors struck: the planned workflow/package.json assertions are contract locks on executable configuration (they go red when packaging or release-attach breaks for a user), the same pattern as the existing `ci` section in test/parsers.test.ts. The negative "no Marketplace publish" assertion is a policy gate on executable CI config enforcing brief Constraint 1, not a prose change-detector; kept, judgment noted.

Convention Compliance: PASS. Tests land in the existing `ci` section of test/parsers.test.ts using the established Node assert + fs.readFileSync contract-lock pattern; no new runner, no new test files. `package`/`prepackage` script naming follows npm pre-script convention (prepackage auto-runs compile).

Dependency Impact: PASS. Lockfile regeneration from a clean tree matches the techContext warning about npm 10 pruning esbuild platform optionals. CI addition (npm run package after compile) is accounted for and depends on the pinned devDependency, which the plan installs. release-please.yaml already has `contents: write`, sufficient for `gh release upload`; reusing the helper-app token for upload matches how the release itself is created. Checkout of the triggering SHA equals the tagged release commit, so the packaged version matches the tag.

Conflict Detection: PASS. No existing packaging or publish setup to duplicate (techContext confirms no vsce and no Marketplace/AMO/CWS jobs). package.json currently has no publisher, no package script, no @vscode/vsce - the plan is purely additive. Pre-release 0.x; no public contract affected. `vsce package` prerequisites (README, LICENSE, repository, engines.vscode) all already present.

Completeness: PASS. All five brief requirements and all four acceptance criteria map to concrete steps with named files. All six planned test behaviors correspond to new executable behavior; prose artifacts correctly untested. Plan's factual claims verified against the repo: `*.vsix` is in .gitignore; .vscodeignore excludes src/, test/, .github/, node_modules/** and release-please config; the `ci` section exists in test/parsers.test.ts; README has a Development section to expand.

Advisory (radical innovation, not applied): add `actions/upload-artifact` of the built `.vsix` to ci.yaml after `npm run package`. The PR packaging smoke already builds the exact artifact; uploading it (short retention) makes every PR sideload-installable for manual testing without a checkout, which directly serves the brief's "sideload before Marketplace exists" intent for one extra step. Operator may evaluate against the cost of a slightly busier CI job.

Advisory (trivial, not applied): CI will compile twice (explicit compile step, then prepackage via npm run package). Harmless; could drop the explicit compile step or accept the redundancy for clearer failure attribution.
