PASS WITH ADVISORY

# Preflight — mobaxterm-active-and-scan-cache (Level 3, re-run)

The current plan passes as written. No plan edits were made.

## TDD Plan Encoding — PASS

All executable units explicitly order stub tests → stub interface → red test run → green implementation. Unit 3 step 2 now leaves `activationEvents` unchanged; the contract test in step 3 can fail before step 4 solely adds `onStartupFinished`. Units 4 and 5 are prose/policy and correctly owe no tests. No change-detectors were scheduled, and no test/production swap was required.

## Convention and Dependency Compliance — PASS

`src/cache.ts` / `test/cache.test.ts` follow the established single-lowercase-word module convention and are wired through the existing `tsx` parser suite. `DiscoverOptions.documentsDir?` is additive, remains in the vscode-free discovery core, and is consumed only through `discoverThemes` → `discoverMobaXterm`. The plan preserves extra-directory inactivity, root-only active detection, `extensionKind: ["ui"]`, and current Mirror picker behavior.

## Conflict Detection — PASS

No cache utility exists to duplicate. `commandMirror` already presents `pickMirrorCandidate` whenever more than one candidate is active, so the new MobaXterm active flag supplies the requested behavior without a redundant UI path. The Known Folder lookup is scoped to the MobaXterm discovery closure, preserving scans that exclude that source.

## Completeness — PASS

All five brief requirements have concrete implementation paths: Known Folder active detection (unit 2), existing multi-candidate picker (verified), matrices (unit 4), process-lifetime cache and empty-cache waiting (units 1 and 3), and startup warming/configuration invalidation (unit 3). The creative documents and tasks consistently use `src/cache.ts` / `test/cache.test.ts`.

## Advisory — Radical Innovation

Consider a future cheap freshness signature: record each scanned root's directory `mtimeMs` with cached results, then re-stat those roots on `load` before serving the cache. This could refresh on additions or removals without a full per-command walk, watcher, worker, or persistent cache. It is intentionally out of scope because the current brief specifies launch/configuration rescans and the additional contract would add complexity.
