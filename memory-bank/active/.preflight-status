PASS WITH ADVISORY

TDD Plan Encoding: the single executable unit (STORE.md + package script + .vscodeignore + parsers.test.ts contracts) orders stub tests, write tests red, then production green. Assertions are packaging contracts (script flag, file existence/non-empty, ignore lines), not phrase/heading change-detectors on STORE.md body. STORE.md copy is prose/policy and correctly has no content tests. No strike or reorder.

Convention: contract tests belong in test/parsers.test.ts (existing ci/packaging section). Manifest and ignore edits match current packaging layout. STORE.md at repo root matches vsce --readme-path.

Dependency: consumers are npm run package, CI package job, and release-please VSIX upload. Plan's post-green step covers compile, parser/host suites, and VSIX inspection. No vscode-free core or apply.ts impact.

Conflict: icon/publisher PNG-magic tests and package.json icon/publisher already exist (prior packaging-icon work; images/icon.png present; .scratch/** already in .vscodeignore). Test Plan item [Icon & Publisher Contract] overlaps that landed work; do not re-implement or duplicate those tests.

Completeness: remaining brief items 3–4, README ignore, STORE.md tests, and package verification have concrete files and steps. Brief items 1–2 (process .scratch/termeleon-logo-1024.png, set icon/publisher) are already satisfied in tree; .scratch source is absent and is not needed if the icon stays as-is. Implementation numbered tests omit the already-green icon assertions; that is consistent with not redoing that work.

Advisories:
- Treat icon generation as already done; follow the STORE.md / --readme-path / README.md-ignore unit only.
- Source-level ignore tests can stay green if vsce still ships README.md; the plan's zip inspect in step 5 is the real packaging gate. vsce maps --readme-path onto extension/readme.md and force-includes that path.
- After build, techContext.md still describes package as `vsce package --no-dependencies` without --readme-path; update that pointer when the script changes (not a plan defect).

Radical innovation (advisory, not applied): replace eyeballing the VSIX with a parsers.test.ts contract that invokes `vsce ls` (same flags as scripts.package) and asserts the file list includes the store readme as extension/readme.md and excludes README.md. That tests the user-visible archive, which is what Open VSX and the Marketplace actually ship.
